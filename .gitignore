# Sidor_box — 独立附属插件（与 Sidor_UI 分开维护）

## 忽略运行时数据与临时产物

# 本地运行数据（不入库）
.sidor-balance.json
.sidor-uploads/
*.probe.json

# 依赖 / 包管理器
node_modules/
pnpm-lock.yaml
pnpm-workspace.yaml

# 日志
*.log

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 临时文件
*.tmp

# 注意：lib/client.js 是构建产物但需要提交（分发输出），不要加进忽略列表
